name = "comment-notifier"
main = "worker.js"
compatibility_date = "2024-01-01"

# 非敏感环境变量（可以提交到 Git）
[vars]
ADMIN_EMAIL = "author@example.com"
FROM_EMAIL = "noreply@example.com"
SITE_NAME = "Your Name"
SITE_URL = "https://example.com"

# 敏感环境变量在 Cloudflare Dashboard 中配置，不要提交到 Git
# 需要在 Dashboard 设置的变量：
# - RESEND_API_KEY
# - NOTIFY_SECRET
